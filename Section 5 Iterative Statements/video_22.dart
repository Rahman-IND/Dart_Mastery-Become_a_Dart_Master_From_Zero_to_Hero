/* Topic : Do while Loop Statement.
It's like post increment operator, 
the value will print first then increment */

void main() {
  // Do while Loop
  var num = 1;
  do {
    print(num);
    num = num + 1;
  } while (num <= 10);
}
